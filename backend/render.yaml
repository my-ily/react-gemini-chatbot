services:
  - type: web
    name: chatbot-backend
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      - key: PORT
        value: 10000

